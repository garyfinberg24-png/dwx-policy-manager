/**
 * Build Information — auto-incremented on each package build.
 *
 * VERSION is the official product version (only changed manually).
 * BUILD_NUMBER is incremented every time `gulp package-solution --ship` is run.
 * Testers can verify they have the correct build by checking the footer.
 *
 * Format: v{VERSION} Build {VERSION}.{BUILD_COUNTER}
 * Example: v1.2.5 Build 1.2.5.1
 */

export const VERSION = '1.2.5';
export const BUILD_NUMBER = '1.2.5.28';
export const BUILD_DATE = '2026-04-10';
